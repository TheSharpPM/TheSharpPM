# Pinned runtime dependencies for scripts/aggregate.py and
# scripts/agent_sharp.py. Pin to specific versions so a malicious or
# breaking release upstream cannot ship into CI without a deliberate
# bump here. Rebump deliberately via Dependabot or manually.
feedparser==6.0.12
requests==2.34.0
groq==1.6.0
langfuse>=3.0.0,<4.0.0
